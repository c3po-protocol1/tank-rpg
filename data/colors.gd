class_name Colors
extends RefCounted

## Brown palette color constants. All visual elements MUST use these.

# Background
const BG_DARK := Color(0.28, 0.22, 0.16)
const BG_MID := Color(0.38, 0.30, 0.22)
const BG_LIGHT := Color(0.50, 0.42, 0.32)

# Terrain
const GROUND_DARK := Color(0.30, 0.24, 0.16)
const GROUND_MID := Color(0.40, 0.32, 0.22)
const GROUND_SURFACE := Color(0.45, 0.38, 0.28)
const GRASS_ACCENT := Color(0.38, 0.42, 0.28)
const CRATER := Color(0.22, 0.18, 0.12)

# Tanks
const TANK_BASIC := Color(0.55, 0.45, 0.35)
const TANK_DEALER := Color(0.60, 0.42, 0.30)
const TANK_TANKER := Color(0.45, 0.40, 0.32)
const TANK_SUPPORT := Color(0.48, 0.52, 0.38)
const TANK_ARTILLERY := Color(0.50, 0.38, 0.28)
const TANK_SCOUT := Color(0.58, 0.50, 0.38)
const TANK_ENEMY := Color(0.52, 0.35, 0.30)
const TANK_BOSS := Color(0.55, 0.28, 0.25)
const TANK_SUBBOSS := Color(0.58, 0.32, 0.28)

# UI
const UI_PANEL := Color(0.25, 0.20, 0.15, 0.9)
const UI_BUTTON := Color(0.55, 0.45, 0.35)
const UI_BUTTON_HOVER := Color(0.62, 0.52, 0.40)
const UI_BUTTON_PRESSED := Color(0.45, 0.38, 0.28)
const UI_TEXT := Color(0.92, 0.88, 0.82)
const UI_TEXT_DIM := Color(0.65, 0.58, 0.48)

# Bars
const HP_FULL := Color(0.45, 0.55, 0.30)
const HP_MID := Color(0.65, 0.55, 0.25)
const HP_LOW := Color(0.65, 0.30, 0.25)
const SP_BAR := Color(0.35, 0.50, 0.52)
const XP_BAR := Color(0.55, 0.48, 0.30)

# Effects
const EXPLOSION_OUTER := Color(0.75, 0.45, 0.20)
const EXPLOSION_INNER := Color(0.85, 0.60, 0.25)
const EXPLOSION_CORE := Color(0.95, 0.85, 0.55)
const MUZZLE_FLASH := Color(0.90, 0.65, 0.25)
const DAMAGE_NUMBER := Color(0.90, 0.85, 0.75)
const HEAL_GREEN := Color(0.40, 0.65, 0.35)
const SHIELD_BLUE := Color(0.40, 0.50, 0.65)

# Clouds
const CLOUD := Color(0.55, 0.48, 0.40, 0.3)
